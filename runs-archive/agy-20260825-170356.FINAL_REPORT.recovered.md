# agy run FINAL_REPORT — recovered fragment

agy-20260825-170356's worktree was swept by a workspace cleanup wave
before its untracked FINAL_REPORT.md could be captured (capture-before-
sweep was added to the plan the same day — one wave too late for this
artifact). The run's CODE survives on branch harness/agy-20260825-170356
(10 commits, 05cb0864..7a2b8baa). What follows is the head of the report
(lines 1-60 of ~120), read verbatim by the muse-spark session during the
three-way comparison before the sweep; everything after section 2 is
lost. The comparison artifacts (muse-spark COMPARISON.md, the neutral
comparison, agy's own 10-line summary quoted by the operator) carry
summaries of the missing sections.

---

# Final Report: yt-is Review-and-Improve Loop (10 Iterations Complete)

**Worktree:** `P:\packages\yt-is\.worktrees\agy-20260825-170356`
**Branch:** `harness/agy-20260825-170356`
**Commit Range:** `05cb0864..7a2b8baa`

## 1. Summary of Changes Made Across All 10 Iterations

### Iteration 1: Ingest Interface Compatibility, Auth Mocks, and Hermetic Test Fixtures
- **`scripts/run_dht_ingest.py`**: Added backwards-compatible `DB = SDB` alias, added `discover_archive()` single-archive discovery helper, updated `discover_archives()` to support `CANDIDATE_GLOBS`, and made `tdb` argument optional in `ingest_archive(archive, tdb=None)` with automatic connection opening and cleanup.
- **`tests/test_c4_auth_validation.py`**: Updated `TestFamilyRefreshUsesLiveSessionCheck` to assert against the active `profile_session_matches_expected` verification flow (following retirement of `sync_worker_profiles`) and bound test fixtures to canonical account profile `a.hominidae` with `YTIS_NLM_EXPECTED_EMAIL`.
- **`tests/test_sharded_lane_series.py`**: Replaced direct dependencies on uncommitted `.logs/sharded_lane_series/*.json` test files in `test_pro_free_lane_config_uses_dedicated_browser_roots`, `test_pro_free_hotmail_lane_config_includes_second_free_account`, and `test_free_only_lane_config_uses_free_account_route` with self-contained `tmp_path` fixtures.

### Iteration 2: Worktree Isolation, Path Invariants, and Extractor Validation
- **`tests/*.py` (30 test files)**: Standardized dynamic `sys.path` injection using `Path(__file__).resolve().parents[1]` rather than hardcoded `P:\packages\yt-is` strings with multi-escaped backslashes, eliminating worktree import bleed and test suite pollution.
- **`csf/cache.py` & `csf/shared_retry_pool.py`**: Normalized `_DEFAULT_SHARED_DB_PATH` and `_DEFAULT_POOL_DB_PATH` to adhere to the forward-slash invariant.
- **`csf/video_utils.py` & `tests/test_video_utils.py`**: Added input parameter validation (`fps > 0` and `max_frames >= 1`) in `extract_frames()` to reject invalid arguments early before spawning FFmpeg subprocesses, with accompanying unit tests.

### Iteration 3: Evidence Fabric Dynamic Overrides and Algorithmic Optimization
- **`ef/authority.py`**: Added `get_transcripts_db_path()` and `get_status_db_path()` helpers with `YTIS_TRANSCRIPT_CACHE_DB_PATH` and `YTIS_BATCH_STATUS_DB_PATH` environment variable override support across `list_eligible_transcripts`, `reopen_span`, and `authority_stats`.
- **`ef/concept_registry.py` & `ef/concept_discovery.py`**: Added `get_catalog_path()` with `YTIS_EF_CATALOG_DB_PATH` environment variable override support for database connection handling.
- **`ef/routing.py`**: Optimized `fuse_identifier_priority` to use dictionary lookups (`lit_rank`) for O(1) tiebreak evaluations instead of repetitive O(N^2) list `.index()` calls inside the sort comparator.

### Iteration 4: Centralized SQLite Connection Factory and Path Registry
- **`csf/paths.py`**: Added centralized database path accessors for `get_catalog_db_path()`, `get_fts_db_path()`, and `get_shared_retry_pool_db_path()` with environment override hooks.
- **`csf/db_utils.py`**: Introduced unified `open_sqlite_ro()`, `open_sqlite_rw()`, `sqlite_ro_scope()`, and `sqlite_rw_scope()` connection helpers with standard 30s busy timeouts and WAL support.
- **`tests/test_db_utils.py`**: Added unit tests covering read-only enforcement, WAL mode, scoped lifecycle, and path environment variable overrides.

### Iteration 5: Subprocess Lifecycle Hardening and Ephemeral Temp Cleanup
- **`csf/sharded_lane_series.py`**: Hardened lane child process termination so that if `proc.wait()` fails or is interrupted by an exception, the child process is cleanly terminated, killed if necessary, and reaped.
- **`csf/video_utils.py` & `tests/test_video_utils.py`**: Added failure-safe cleanup for auto-created frame directories so extraction errors do not orphan temporary directories in `%TEMP%`, protected against deleting parent temp roots.

### Iteration 6: Cache Connection Timeout & Busy Pragma Hardening
- **`csf/cache.py`**: Enforced 30.0s lock timeout and `PRAGMA busy_timeout = 30000` across `_connect_shared_db()`, `promote_staging_cache()`, and `backup_transcript_cache()`, preventing `database is locked` OperationalErrors during concurrent cache promotions.

### Iteration 7: Status Storage Connection Scoping & Busy Pragma Standardization
- **`csf/batch_status.py`**: Standardized 30.0s connect timeout and `PRAGMA busy_timeout = 30000` across `_get_conn()`, `set_granular_status()`, `get_transcript_status()`, and `get_visual_status()`.

### Iteration 8: NotebookLM Client Profile Directory Isolation & Probe Formatting
- **`csf/nlm_client.py`**: Added `NOTEBOOKLM_PROFILES_DIR` environment override support to `DEFAULT_PROFILE_STORAGE_ROOT` for isolated hermetic testing, and cleaned session probe reason strings.

### Iteration 9: Evidence Fabric Freshness Connection Scoping
- **`ef/freshness.py`**: Replaced direct hardcoded SQLite connection strings with `authority.get_transcripts_db_path()` and `open_sqlite_ro` in `authority_watermark()` and `compute_lag()`.

### Iteration 10: CLI Verification Script Robustness & Windows Path Support
- **`scripts/verify_transcript_storage.py` & `tests/test_verify_transcript_storage.py`**: Replaced raw string interpolation in SQLite `ATTACH DATABASE` with POSIX-normalized URI parameters, added `open_sqlite_ro` connection wrapping with guaranteed `try...finally` cleanup, graceful handling for missing databases, and complete unit tests.

## 2. Test Results Before and After

| Metric | Baseline | Final |
| :--- | :--- | :--- |
| **Total Tests Discovered** | 2,253 | 2,260 |
| **Passed** | 2,224 | **2,252** |
| **Failed** | 6 | **0** |
| **Errors** | 15 | **0** |
| **Skipped** | 8 | 8 (retained live artifacts swept) |

*[sections 3+ lost to the worktree sweep; summaries survive in the
muse-spark COMPARISON.md and the neutral comparison]*
