You are reviewing a Python module from a production YouTube transcript pipeline. The main function `fetch_transcript_chain` is 200+ lines with 5 nested closure functions, inline fallback logic, a mutable global, and mixed concerns (validation, logging, caching, rate-limiting, translation, and orchestration all in one function body).

Here is the function signature and the start of the problematic code:

```python
def fetch_transcript_chain(
    video_id: str,
    config: LanguageConfig,
    *,
    skip_notebooklm: bool = False,
    admission_metadata: dict[str, object] | None = None,
) -> TranscriptResult:
    """Fetch transcript using the full fallback chain.
    Chain order: oEmbed → yt-dlp → yt-dlp+cookies → direct_api → NotebookLM → Selenium → Whisper
    """
    prefer_lang = config.prefer_lang

    # Inline validation (should be extracted)
    if not _validate_video_id(video_id):
        return TranscriptResult(video_id=video_id, lang=prefer_lang, raw_lang=None, ...)

    try:
        _validate_bcp47(prefer_lang)
    except ValueError:
        return TranscriptResult(..., error=f"invalid BCP-47: {prefer_lang!r}", ...)

    chain_started_at = time.perf_counter()

    # Nested closure 1: error classifier — pure logic trapped inside orchestrator
    def _classify_failure(error: str | None, stage: str) -> str:
        if not error: return "unknown"
        err_lower = error.lower()
        if "429" in err_lower or "rate limit" in err_lower: return "quota_exceeded"
        if "region" in err_lower or "not available" in err_lower: return "region_block"
        if "auth" in err_lower or "login" in err_lower: return "auth_failed"
        if "captcha" in err_lower or "bot detection" in err_lower: return "captcha"
        if "timeout" in err_lower: return "timeout"
        if "no transcript" in err_lower: return "no_transcript"
        if "source add failed" in err_lower: return "source_add_failed"
        if "no speech detected" in err_lower: return "no_transcript"
        if "unavailable" in err_lower or "deleted" in err_lower: return "unavailable"
        if "not found" in err_lower or "404" in err_lower: return "unavailable"
        return "unknown"

    # Nested closure 2: result builder
    def _none_result(last_err, last_stage):
        return TranscriptResult(..., failure_reason=_classify_failure(last_err, last_stage or ""))

    # Nested closure 3: failure archiver (side effects inside closure)
    def _archive_failed_result(last_err, last_stage):
        failure_reason = _classify_failure(last_err, last_stage or "")
        _log_transcript_chain_event("transcript_chain_failed", ...)
        if failure_reason == "unavailable":
            _persist_terminal_failure(video_id, last_err, last_stage)
        else:
            _record_soft_negative(video_id, failure_reason, ...)
        return _none_result(last_err, last_stage)

    # Nested closure 4: stage timing logger
    def _stage_started(source, lang=None):
        started_at = time.perf_counter()
        _log_transcript_chain_event("transcript_stage_started", ...)
        return started_at

    # Nested closure 5: stage completion logger
    def _stage_completed(source, started_at, *, success, error=None, chars=0, lang=None, skipped=False, skip_reason=None):
        _log_transcript_chain_event("transcript_stage_completed", ...)

    # The main loop: iterates fallback methods with inline special-casing
    for source, fetch_fn, stage in methods_to_try:
        if _is_source_rate_limited(source):
            continue  # circuit breaker
        if source in {_SOURCE_SELENIUM, _SOURCE_WHISPER} and not expensive_fallback_enabled:
            # ... log skip, continue
            continue

        # Special case: Whisper has admission check + global mutable state
        if source == _SOURCE_WHISPER:
            global _WHISPER_ENABLED  # ← mutable module global inside loop
            if _WHISPER_ENABLED is None:
                _WHISPER_ENABLED = os.getenv("YTIS_WHISPER_ENABLED", "true").lower() == "true"
            if not _WHISPER_ENABLED:
                continue
            should_attempt, reason, err = _whisper_admission_check(admission_metadata)
            if not should_attempt:
                # ... 10 lines of failure handling inline
                return _none_result(err, "whisper_admission")

        # Special case: NLM ignores language fallback, calls with "en" only
        if source == _SOURCE_NLM:
            success, transcript, error = fetch_fn(video_id, "en")
            if success and transcript:
                # ... 20 lines of success handling: translation, caching, result building
                return result

        # Generic path: iterate language fallbacks
        for lang in lang_fallbacks:
            success, transcript, error = fetch_fn(video_id, lang or "")
            if success and transcript:
                # ... 15 lines of success handling (duplicated from NLM path)
                return result

        last_error = error

    return _archive_failed_result(last_error, last_stage_reached)
```

**Problems to address:**

1. **5 nested closures** trap pure logic (`_classify_failure`), result builders (`_none_result`), and side-effecting functions (`_archive_failed_result`, `_stage_started`, `_stage_completed`) inside the orchestrator. None of them need to be closures.

2. **Mutable global `_WHISPER_ENABLED`** is read/written inside the loop body. Race condition risk under concurrent access.

3. **Duplicated success handling** — the NLM path and the generic path both do: translation check → build TranscriptResult → cache → return. ~15-20 lines duplicated.

4. **Mixed concerns** — validation, error classification, logging, caching, rate-limiting, translation, and orchestration are all in one function.

5. **The fallback loop has 3 special cases** (Whisper admission, NLM language override, expensive fallback gating) inline in the loop body, making the control flow hard to follow.

**Task:** Design a refactoring plan for this function. Show:
1. Which closures should be extracted to module level (and why)
2. How to eliminate the duplicated success handling
3. How to handle the special cases without inline conditionals in the loop
4. The new function signature(s) after refactoring

Do NOT write the full implementation. Write the refactoring PLAN — class/function signatures, responsibility assignments, and the key design decision for each extraction. Explain WHY each change improves the code.
