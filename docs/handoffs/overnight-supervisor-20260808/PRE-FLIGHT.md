# Offline Overnight Preflight

Status: ready for bounded offline implementation
Generated by parent preflight: 2026-08-08

## Source Of Truth

- Worktree: `P:/.worktrees/yt-is-autonomous-overnight-20260808`
- Branch base: current committed yt-is checkpoint `c98a17b` (`checkpoint: preserve auth and adaptive scheduler work`)
- Governing package instructions: `AGENTS.md`, `CLAUDE.md`, `HANDOFF.md`
- Current work must remain isolated here. The shared `P:/packages/yt-is` checkout is not an edit target.
- Current user boundary: offline work only. No NotebookLM/Gemini, YouTube/API, authentication, quota, live benchmark, destructive command, commit, or push.

## Existing Context

`HANDOFF.md` identifies transcript import/forward-sync/unmatched work as the active integration stream. It also records that the bounded adaptive worker scheduler is implemented and has offline tests, but has no live performance proof. The three descriptive auth identities and their storage paths are already documented; do not revive deprecated profile names or redesign auth during this run.

## Ranked Offline Candidates

1. Inspect the current transcript import forward-sync/unmatched path and implement one narrow missing behavior or semantic test if the source and tests prove a gap.
2. If the import path has no justified gap, strengthen an existing adaptive-scheduler invariant, reducer, or offline test without changing live scheduling behavior.
3. If neither code path has a justified change, produce a decision packet documenting the evidence gap and the smallest next offline action.

## Recommended Branch

Start with candidate 1. Read the actual import implementation, callers, state/database ownership, and tests before editing. Falsifier: the claimed import gap is already handled on the consumed path and covered by a semantic test. Stop condition: no evidence-backed candidate remains, or the proposed change would require live auth, external data, quota, or a throughput run.

## Claim Ledger

| Claim | Type | Evidence | Falsifier | Action allowed |
|---|---|---|---|---|
| Transcript import/forward-sync/unmatched work is the active integration stream | historical_context | current `HANDOFF.md` | current source/handoff contradicts it | inspect only |
| Adaptive scheduler code exists with offline coverage | historical_context | current `HANDOFF.md` and package tests | source/test inspection disproves it | inspect or improve offline |
| A higher sustained VPH result is not established | verified_fact from project contract | current handoff/throughput docs | current canonical benchmark packet proves otherwise | no live run |
| A narrow import or scheduler improvement may be justified | hypothesis | requires source/test inspection | no semantic gap found | implement only if falsifiable |

## Required Handoff

The implementation phase must write `IMPLEMENTATION.md` with exact files, evidence, verification run or unavailable, remaining risks, and a status of `done`, `partial`, `blocked`, or `not-started`. It must not claim optimality or live readiness from this preflight.
