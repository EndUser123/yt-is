# C5 battery — STOP packet (25/26 gates PASS; NOT promoted)

F-gate rule: any failure -> STOP, gates frozen, no set reuse.

## The 25 passing gates (phase-1 + judged)

Every exact/identifier stratum at 1.0 including the F-gate centerpiece:
- exact_df1_STRONG R@1 = 1.0 (n=31); df2-10/df11-100/df101-1000/punct
  literal-prefix 1.0
- **weak_df1 (hizoJc-class) top3-literal 1.0 AND literal-prefix 1.0 (n=14)**
- zero-df empty 1.0; twins 0; TikTok/YouTube/OpenAI routed ambiguous;
  Google/Python/Windows/Instagram routed semantic
- judged: weak_common any@3 = 1.0; telegraphic 0.833 (gate 0.70);
  semantic_technical 0.846 (gate 0.75)
- latency p95 131 ms; reopen 30/30; filter 20/20; parity; namespace;
  lag 0; restart/reconnect; all four regression preconditions
  (C1 strong df1 1.0; C2 strong literal 1.0; twins 0; natural 0.7167)

## The one failure

**comparison_questions judged any@3 = 0.70 vs gate 0.75 (n=10).**
Misses: "tldr versus man pages" (the relevant video ranks r1 under the
telegraphic phrasing but not under comparison phrasing), "keynote
speaker perks versus checklists", "scriptsharp versus typescript
history" (the Anders Hejlsberg origin video exists in the index and was
retrieved under technical phrasing, ranked 4th+ here). Authored MRR for
the stratum was also lowest (0.381).

- Stage: semantic ranking under explicit "X versus Y" phrasing — the
  comparison frame pulls generic contrast videos above the specific
  relevant one.
- Leading explanation: comparison phrasing is the hardest query class
  for the current fusion (consistent across C3 0.339, C4 0.339,
  C5 judged 0.70); n=10 means one query = 0.10, so 0.70 vs 0.75 is a
  two-query margin on the smallest stratum.
- Strongest alternative: judgment noise (borderline calls on 2 rows).
- Distinguishing test: re-run comparison queries with dense-only vs
  fusion (component baselines exist in same_corpus_baselines).
- Smallest remediation (operator's call): (i) accept the measured
  comparison-class reality and preregister C6 with a comparison gate
  derived from three batteries of comparison evidence (0.339/0.339/0.70
  judged) — e.g., judged any@3 >= 0.65 with n >= 20 for resolution;
  (ii) targeted improvement: comparison-phrasing query expansion or the
  sparse-heavier fusion (measured within noise earlier — re-measure on
  a comparison-specific set); (iii) treat comparison as informational
  (product decision that comparison queries are best-effort).

## State

Generation 1 inactive. No vectors/storage touched. C1-C5 all sealed
regression/development evidence. The F-gate ambiguous lane itself is
fully validated (weak_df1 1.0/1.0, weak_common judged 1.0).

agent: zcode · 2026-08-17
